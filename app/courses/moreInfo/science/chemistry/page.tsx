import React, { Suspense } from 'react'
import MoreInfo from '@/components/coursesInfo/science/chemistry/moreInfo'
import Loading from '@/app/loading'

type Props = {}

const page = (props: Props) => {
  return (
    <div><MoreInfo/></div>
  )
}

export default page